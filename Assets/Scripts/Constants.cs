using UnityEngine;

public static class Constants
{
    /// <summary>
    /// Used to determine keycode that is used as not clicked indicator in
    /// PlayerMovement script
    /// </summary>
    public const KeyCode NotClicked = KeyCode.Escape;
}